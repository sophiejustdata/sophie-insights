// Plesk / Passenger startup file.
// The production build (`npm run build`) emits a Node server bundle at
// .output/server/index.mjs which starts an HTTP server on process.env.PORT.
import "./.output/server/index.mjs";
